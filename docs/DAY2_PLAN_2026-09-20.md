# Day 2 Plan & Validation Report — 2026-09-20 (final day, 5 submissions)

## TL;DR
Day 1 gave us a validated risk ensemble (v4 = 0.81854, +0.0072 over the ported floor). Overnight we
ran a strict OOF validation program on the 2-CPU/4GB box, **rejected three tempting dead ends**, and
produced **7 pre-validated submission files** with a 12-point assertion battery. Every file is staged
and ready; submission order follows a branch tree designed so that the first result determines the
safest path for the remaining four.

## What was validated overnight (all on honest out-of-fold data)

### Rejected ideas (negative results — do not revisit)
| Idea | OOF result | Verdict |
|---|---|---|
| AP@5 lambda-rank objective (V12 port) for evidence | MAP@5 0.4912 vs 0.5189 binary | fold-0 "0.5621" was a fluke; dead |
| sus_ hand-model aggregates as pair-model features | clean AP 0.98613 → 0.98428 | exclusion was correct |
| Adding `hand_sus` to the RAW evidence blend | 0.5215 vs 0.5217 | scale mismatch kills it |
| Searched blend weights (7 experts) | LB 0.81612 < v4 0.81854 | v5 lesson: search overfits |

### Adopted improvements (positive results)
1. **4-seed hand-ranker bag** (was 2): OOF global MAP@5 0.5145 → **0.5201**.
2. **Rank-normalized evidence blend**: `0.70·rank(global) + 0.30·rank(spec / prior)` =
   **0.5294 OOF MAP@5** (+0.0077 over the production raw blend). Sus weight re-tested after the
   stronger bag — no longer helps, dropped (simpler).
3. **Equal-weight CatBoost family** (6 cats, no weight search): clean OOF AP 0.98693 vs v4's
   0.98589; `+0.10 lgb_sub60` variant reaches 0.98725. Modest but honest.
4. **Behavior rho cut on the final recipe risk rank** (matches all LB-proven submissions).

## Pre-staged submissions (12/12 green lights each)
Format battery: shape/columns, pair_id set, risk range+uniqueness, behavior domain, no NaN,
no duplicate evidence in row, evidence ⊂ shared eval hands, evidence ⊂ evaluation phase,
both pair members seated (seats.parquet), NO_EVIDENCE trailing-only, top-risk pairs have
evidence, CSV round-trip.

| File | Risk | Evidence | Purpose |
|---|---|---|---|
| sub_day2_s1.csv | = v4 exactly (spearman 1.0) | **new rank-blend** | **controlled evidence experiment** |
| sub_day2_s2.csv | 0.2·PU + 0.8·mean(6 CATs) | new | honest family ensemble |
| sub_day2_s3.csv | 0.15·PU + 0.75·cats + 0.10·lgb_sub60 | new | best honest clean-AP |
| sub_day2_s4.csv | 0.3·PU + 0.7·CAT d6 | new | d6 ladder probe |
| sub_day2_s2v4ev / s3v4ev / s4v4ev.csv | as above | **old v4 evidence** | contingency branch |

Cross-checks: s2/s3/s4 risk correlate 0.96–0.98 with v4; top-5,627-set jaccard 0.91–0.94; evidence
overlap 0.8/5 hands per pair vs v4 (31% of pairs unchanged) — meaningful but not wild drift.

## Branch tree for the day
1. **Submit s1** first. It differs from v4 (0.81854) *only* in evidence → the score delta is a pure
   evidence measurement (expected ≈ +0.0015 from OOF).
2. If **s1 > v4**: evidence improved → submit s2, s3, s4; final slot = rank-average of the two best
   risk recipes (built on demand from cached models).
3. If **s1 < v4**: evidence regressed (dev overfit) → switch to the contingency files
   (new risk × old evidence) for the remaining slots; reserve the last slot for the best single
   risk recipe confirmed by the day's results.
4. Any score > 0.83065 becomes the new personal best (old bank stays selected for private scoring).

## Risk notes
- Cat-family ensembles use equal weights only — no metric-driven weight search (v5 lesson).
- All evidence candidates re-scored with the 4-seed models over 9.51M pair-hand rows; top-5 cached
  and byte-identical across the four new-evidence files.
- The 4 GB box OOM-killed the first two assembly passes; the final pipeline is polars-native with
  vectorized validations (the earlier tuple-set validation check alone needed ~2 GB).
